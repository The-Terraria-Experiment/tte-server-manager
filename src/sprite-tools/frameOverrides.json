{
	"_comment": "Manual overrides for pack.mjs's animated-sprite frame detection: \"<itemId>\": <frameCount>. The structural detector gets these right unaided on 1.4.5.6 (102 items, matching the in-game animated set), so this is normally empty -- it exists so a bad detection can be corrected without touching the detector."
}
